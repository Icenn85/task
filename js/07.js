const btnAdd = document.querySelector('button[data-add]');
const btnSubtrac = document.querySelector('button[data-sub]');
const btnDiv = document.querySelector('button[data-div]');
const btnMultip = document.querySelector('button[data-multip]');
const inputFirst = document.querySelectorAll('input[name="value_1"]');
const inputSec = document.querySelectorAll('input[name="value_2"]');
const result = document.querySelectorAll(".form__result");

console.log(btnAdd);

// btn.addEventListener("click", () => {
//     switch (btnAdd)
// })